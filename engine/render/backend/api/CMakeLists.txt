if(CHIRA_RENDER_BACKEND MATCHES "GL")
    list(APPEND CHIRA_ENGINE_HEADERS
            ${CMAKE_CURRENT_LIST_DIR}/BackendGL.h)
    list(APPEND CHIRA_ENGINE_SOURCES
            ${CMAKE_CURRENT_LIST_DIR}/BackendGL.cpp)
elseif(CHIRA_RENDER_BACKEND STREQUAL "SDLRENDERER")
    list(APPEND CHIRA_ENGINE_HEADERS
            ${CMAKE_CURRENT_LIST_DIR}/BackendSDL.h)
    list(APPEND CHIRA_ENGINE_SOURCES
            ${CMAKE_CURRENT_LIST_DIR}/BackendSDL.cpp)
elseif(CHIRA_RENDER_BACKEND STREQUAL "DX11")
	list(APPEND CHIRA_ENGINE_HEADERS
            ${CMAKE_CURRENT_LIST_DIR}/BackendDX11.h)
    list(APPEND CHIRA_ENGINE_SOURCES
            ${CMAKE_CURRENT_LIST_DIR}/BackendDX11.cpp)
endif()
